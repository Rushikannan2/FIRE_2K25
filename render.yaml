services:
  - type: web
    name: CryptoQ
    env: python
    buildCommand: "pip install -r requirements.txt && python create_empty_models.py"
    startCommand: "python start_django.py"
    rootDir: .
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: CryptoQWeb.settings
      - key: PYTHONPATH
        value: /opt/render/project/src/CryptoQWeb