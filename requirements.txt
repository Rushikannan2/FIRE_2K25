# Optimized requirements.txt for Render deployment
# CPU-only builds to minimize size and build time

# Core Django
Django==4.2.7
djangorestframework==3.14.0
gunicorn==21.2.0
psycopg2-binary==2.9.9

# PyTorch CPU-only builds (smaller, faster)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
torchvision==0.15.2+cpu

# ML Libraries (lightweight versions)
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
transformers==4.30.2

# Image processing
Pillow==10.0.0

# Visualization (optional - can be removed if not needed)
plotly==5.15.0
matplotlib==3.7.1

# Text processing (optional - can be removed if not needed)
wordcloud==1.9.2

# Hugging Face integration
huggingface_hub==0.16.4

# HTTP libraries
requests==2.31.0
urllib3==2.0.4

# Static files middleware for production
whitenoise==6.6.0

# Database configuration for Render
dj-database-url==2.1.0